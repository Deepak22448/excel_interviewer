# AI Model Configuration
# The system will use OpenAI if OPENAI_API_KEY is provided, otherwise it will fall back to Google AI

# Option 1: OpenAI (Primary choice - uses GPT-4o model)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Option 2: Google AI (Fallback - uses Gemini 2.5 Flash model)
# Get your API key from: https://aistudio.google.com/
GOOGLE_GENERATIVE_AI_API_KEY=your_google_ai_api_key_here

# Note: You only need ONE of the above API keys. The system prioritizes OpenAI if available.